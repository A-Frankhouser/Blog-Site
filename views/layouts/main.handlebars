<!DOCTYPE html>
<html lang="en">


<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
        {{!-- Bootstrap --}}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css" />

    <title>The Blog Spot!</title>
        {{!-- CSS --}}
    <link rel="stylesheet" href="/css/style.css">
        {{!-- Bootstrap --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Gentium+Book+Plus&family=Quicksand:wght@300&display=swap"
        rel="stylesheet">
</head>

<body>
<div>
    <header>
        {{!-- "The Blog Spot hero" --}}
        <h1 class="container-fluid text-center bg-success p-2  bg-opacity-75 lh-lg text-white">The Blog Spot!</h1>

        {{!-- Navbar --}}
    <nav class="navbar navbar-expand-lg bg-success p-1 bg-opacity-10" id="navbar">
        <div class="navbar">
            {{#if logged_in}}
            <a href="/dashboard" class="nav-item">Dashboard</a>
            <a href="/" class="nav-item">Homepage</a>
            <button id="logout" class="no-button btn btn-secondary nav-item">Logout</button>

            {{else}}
            <a href="/">Homepage</a>
            <a href="/login">Login</a>
            {{/if}}
        </div>
    </nav>
    </header>

    <main>
        {{{ body }}}
    </main>

    <footer>
        
    </footer>
</div>
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>
</html>